using System;
using System.Collections.Generic;
using System.Text;

namespace CSharpCourse.Fundamentals {
    class Comments {
        public static void Execute() {
            // Beware of unnecessary comments, like the one beneath
            Console.WriteLine("A clean code is always better");
            /* This
             * is a
             * multilne
             * comment
             */
            Console.WriteLine("Multiple lines comments are always useful (see in code)");
        }
    }
}
